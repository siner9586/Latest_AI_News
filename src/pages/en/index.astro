---
import Base from '../../layouts/Base.astro';
import BriefDashboard from '../../components/BriefDashboard.astro';
import { latestBrief } from '../../utils/data';
const b = latestBrief;
---
<Base title={`English AI News Brief · ${b.date}`} description={b.overview_en}>
  <BriefDashboard b={b} lang="en" subtitle={b.overview_en} />
</Base>
